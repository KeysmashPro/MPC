# MPC
Vulkan program prototype

- You'll need the Vulkan & GLFW toolchain to build this.
- Also you'll need to install Slang shader language compiler,
install it with your package manager or with make:
```console
make install-toolchain
```


Build with `make`  
Run with `make run`
